# AGENT-155 — `_audit_tmp/` & `_github-backups/` Audit

**Date:** 2026-04-29 | **Agent:** 155 | **Scope:** scratch/temp dirs, PII leakage, staleness, gitignore posture
**Worktree:** `.claude/worktrees/objective-merkle-40ff93`

---

## 1. TL;DR

| Dir | Tracked files | Disk | First committed | Status |
|---|---|---|---|---|
| `_audit_tmp/` | **48** (41 root + 7 menu_scan; 1 generated `analysis.json` untracked) | **2.0 MB** | `77734df` (2026-04-18) | Stale scratch, **committed**, **NOT gitignored** |
| `_github-backups/` | **0** | 0 B (3 empty subdirs) | `77734df` (2026-04-18) | Empty placeholder, no `.gitkeep`, **NOT gitignored** |

**Verdict:** No PII or secrets leaked. Both dirs are cruft from a Phase-1B audit run 11 days ago. They should be **removed from the repo** (or at minimum gitignored if regenerated locally). `.gitignore` already has the right pattern shape (`_audit-clones/`, `_external-backups/`) — just missing these two.

---

## 2. PII / Secret Scan — `_audit_tmp/`

Ran four pattern sweeps across all 49 files (~2 MB):

| Pattern | Hits | Verdict |
|---|---|---|
| Email regex `[\w.+-]+@[\w.-]+\.\w{2,}` | **0** | Clean |
| `password\|secret\|api[_-]?key\|token\|bearer\|credential` | 103 (10 files) | **All route names** (`/api-keys`, `ai-api-keys.ts`, `tokens` table) — no values |
| Israeli phone `05X-XXXXXXX` / `+972` / 9-digit | 19 | Route paths (`/api/leads/9`), no real numbers |
| `SSN\|teudat\|passport\|credit_card\|iban\|cvv` | **0** | Clean |
| Long token-like strings `[A-Za-z0-9_-]{30,}` (sampled `_hidden.json`) | 0 | Clean |
| `"name":"…"` field values | All match table names (`customers`, `contacts`, `quotes`) — schema metadata, no people | Clean |

**Conclusion:** Files contain only route paths, table/column names, file paths, route mounts, orphan-detection output. **No PII, no real secrets, no customer data.** Safe in repo from a privacy standpoint — the issue is hygiene, not exposure.

---

## 3. `_audit_tmp/` Contents — What Is It?

Output of a Phase-1B static-analysis sweep against the codebase, snapshotted on 2026-04-18 and never regenerated:

- **Route/menu scans:** `app_routes.json` (122 KB), `menu.json` (302 KB), `menu_with_parent.json` (168 KB), `menu_gap.json`
- **API/DB inventories:** `_scan_api.json` (518 KB — biggest file), `_scan_db.json`, `tables.json`, `table_coverage.json`, `api_mounts.json`, `api_analysis.json`, `_rpcs.json`
- **Orphan/dead-code reports:** `_orphans.json`, `_orphans2.json`, `_orphans_split.json`, `_orphans_final.json`, `_orphans_dead_v3.json`, `_orphans_dead_final.json`, `dead_links.json`, `_unmounted.json`, `_unused.json`, `_truly_missing.json`, `broken_refs.json`, `orphan_pages.json`
- **Categorization drafts:** `_categorized.json`, `miscategorized.json`, `to_add.json`, `to_recategorize.json`, `_hidden.json`
- **Scanner scripts** (committed alongside outputs): `_build_report.js`, `dead_links.js`, `api_calls_scan.js`/`_scan2.js`, `orphan_scan.js`/`_scan2.js`, `menu_scan/build_report.py`, `menu_scan/analyze.py`

**Staleness indicators:**
- All files dated 2026-04-18 in commit `77734df` ("phase-1b: recovery package + 22 control files…"). Repo has had 5+ feature commits since.
- File names suggest **iterative drafts** (`_orphans.json`, `_orphans2.json`, `_orphans_final.json`, `_orphans_dead_v3.json`, `_orphans_dead_final.json` — 5 versions of the same report). Final-of-finals pattern = working scratch, not artifacts.
- Underscore-prefix convention (`_categorized.json`, `_fe.json`) is the project's "internal/scratch" marker (matches `_merge-staging/`, `_qa-reports/`, etc. — most of which **are** gitignored or in `.gitignore`-spirit).

---

## 4. `_github-backups/` Contents — What Is It?

Three **empty** subdirectories: `desktop-tutorial/`, `kobi-erp/`, `technokoluzi-erp/`. No `.gitkeep`, no files, no commits ever added content. Names suggest staging slots for cloning external GitHub backups (compare `_audit-clones/` and `_external-backups/` — both already in `.gitignore`).

Git tracks the three dir entries indirectly (commit `77734df` created them, but Git only stores files). Since they're empty, `git ls-files` returns nothing — they exist only on disk.

---

## 5. `.gitignore` Posture (current)

`.gitignore` already excludes the sibling concept dirs:

```
_audit-clones/
_external-backups/
```

…but **NOT** `_audit_tmp/` or `_github-backups/`. Inconsistent.

---

## 6. Recommendations (priority order)

### P0 — Remove from repo
```bash
git rm -r _audit_tmp/ _github-backups/
git commit -m "chore: remove stale Phase-1B audit scratch + empty github-backups slots"
```
Saves 2 MB, ~50 files of working scratch. Reports were superseded by `_qa-reports-25/` (current, dated today).

### P1 — Add to `.gitignore` (in case regenerated locally)
Append to `.gitignore`:
```
# Static-analysis scratch (regenerated by audit scripts, not artifact-quality)
_audit_tmp/
_github-backups/
```

### P2 — If audit scripts are still needed
The scanner sources (`_build_report.js`, `menu_scan/build_report.py`, etc.) live inside the deleted dir. If they're meant to be reusable, **move to `scripts/audit/`** (a tracked location) and have them write output to `_audit_tmp/` (gitignored). Output is scratch; the scripts themselves are tooling.

---

## 7. Risk Summary

| Concern | Severity | Status |
|---|---|---|
| PII leak | None | No emails, IDs, phones, payment data found |
| Secret leak | None | All hits are route/path strings, no values |
| Stale data misleading consumers | **Low–Medium** | 5 versions of `_orphans*.json` could be mistaken for current state |
| Repo bloat | Low | 2 MB, but compounds — sets precedent for committing scratch |
| Gitignore inconsistency | Low | `_audit-clones/` / `_external-backups/` ignored but `_audit_tmp/` / `_github-backups/` not |

**Bottom line:** safe but messy. Delete + gitignore.
