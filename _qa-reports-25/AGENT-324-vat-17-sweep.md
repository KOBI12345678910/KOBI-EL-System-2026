# AGENT-324 — VAT 17% Sweep (post-2026-01-01)

**Date:** 2026-04-29   **Today's IL VAT rate:** **18%** (effective 2026-01-01)
**Goal:** locate every literal `17%` / `0.17` / `VAT_RATE = 0.17` left in *live* code that should now be 18% or driven by `getVatRateForDate(docDate)`.

**Patterns swept:** `0\.17`, `vat.?rate.*=.*17`, `VAT_RATE.*=.*0?\.17`, `MAAM.*17`, `"17%"`, `'17%'`.

**Recommendation legend:**
- **STALE** — flat-out wrong; flip to 0.18 / "18%".
- **HELPER** — replace the literal with `getVatRateForDate(date)` from `erp-app/src/utils/money.ts` or `packages/shared-tax/src/vat.ts`.
- **HISTORICAL** — keep as-is (legacy fallback for pre-2026 invoices, vat_rates lookup table, comparison engine).
- **DOC** — non-code (markdown / log); update copy or leave for archives.

---

## A. Critical source code (must change)

### A1. `api-server/src/constants.ts:1`
```ts
export const VAT_RATE = 0.17;
```
**STALE.** This is THE primary backend VAT constant. **Recommendation:** delete the file (or re-export `VAT_RATE_CURRENT` from shared-tax) and migrate all importers to `getVatRateForDate(docDate)`. Already flagged in AGENT-271 / AGENT-19. Highest blast-radius bug remaining.

### A2. `api-server/src/lib/contractor-decision.ts:1`
```ts
export const VAT_RATE = 1.17;
```
**STALE.** Used as a **gross-up multiplier** (1 + rate). For 18% it must be `1.18`. Files that import this — re-check after fix. **Recommendation:** replace with `1 + getVatRateForDate(invoiceDate)`.

### A3. `api-server/src/lib/pricing-engine.ts:100`
```ts
export const IL_VAT_RATE_PRIOR = 0.17;
```
**HISTORICAL — keep.** Sibling of `IL_VAT_RATE = 0.18` on L98. Used for legacy invoice recalculation. Verify no `IL_VAT_RATE_PRIOR` reference is feeding new (post-2026) calc paths.

### A4. `api-server/src/lib/project-costing-engine.ts:577`
```ts
const vatRate = parseFloat(project.vat_rate) || 17;
```
**STALE default.** Falls back to `17` (interpreted as percent) when `project.vat_rate` is empty. **Recommendation:** `parseFloat(project.vat_rate) || (getVatRateForDate(project.start_date) * 100)` — or just `|| 18` if no historical project-costing reruns expected.

### A5. `erp-app/src/utils/money.ts:6`
```ts
const VAT_RATE_PRE_2026 = 0.17;
```
**HISTORICAL — keep.** This is the canonical helper file: `VAT_RATE_2026 = 0.18` on L7, exported via `getVatRateForDate()`. Correct as-is. (AGENT-302 noted this is duplicated vs. `packages/shared-tax/src/vat.ts` — separate dedup task, not a 17% bug.)

### A6. `erp-app/src/pages/procurement/raw-materials/weight-calculator.tsx:18`
```ts
const VAT_RATE = 0.17;
```
**STALE.** New quotes generated by the weight calculator will under-charge by 1 percentage point. **Recommendation:** `import { getVatRateForDate } from "@/utils/money"; const VAT_RATE = getVatRateForDate(new Date());` — or just `0.18`.

### A7. `erp-app/src/pages/settings.tsx:127`
```tsx
<Input ... placeholder="17" step="0.01" />
```
**STALE placeholder.** UI hint text shows `17` to a 2026 user. **Recommendation:** `placeholder="18"`.

### A8. `api-server/src/__tests__/unit/invoice-calculations.test.ts:3, :65, :75, :111, :189`
```ts
const VAT_RATE = 0.17;
// describe("Invoice totals with VAT 17%")
// it("applies 17% VAT correctly")
// expect(...).toBe(Math.round(800 * 0.17 * 100) / 100);
// expect(...).toBe(Math.round(6602.5 * 0.17 * 100) / 100);
```
**STALE TEST FIXTURE.** Either (a) tests passing against stale prod logic and masking bugs, or (b) tests will start failing when prod is fixed. **Recommendation:** Drive off `getVatRateForDate(fixture.invoiceDate)`; pin the date to 2026-04-15 (today-ish) so the fixture self-updates with the helper. Already flagged in AGENT-19, AGENT-195, AGENT-271, AGENT-302, AGENT-307.

### A9. `api-server/src/__tests__/integration/financial-flow.test.ts:3, :188, :211, :403`
```ts
const VAT_RATE = 0.17;
// it("applies 17% VAT to before-VAT amount")
// expect(quote.vatAmount).toBe(Math.round(800 * 0.17 * 100) / 100);
// expect(quote.vatAmount).toBeCloseTo(10925 * 0.17, 0);
```
Same as A8. **Recommendation:** same fix.

### A10. `api-server/src/routes/quote-builder.ts:552`
```ts
["VAT 17% / Maam:", fmtC(vat)],
```
**STALE.** PDF/print row label hardcoded "17%". Customer-visible. **Recommendation:** `[\`VAT ${(rate*100).toFixed(0)}% / Maam:\`, fmtC(vat)]` using the actual quote's rate.

### A11. `api-server/src/__tests__/.../prompts.ts` (and `onyx-ai/agents/src/llm/prompts.ts:22`, `AI-Task-Manager/artifacts/kobi-agent/src/llm/prompts.ts:22`)
```ts
3. מע"מ 17%
```
**STALE.** LLM system prompt teaches the AI agent to compute 17% VAT. Direct user-visible bug. **Recommendation:** `מע"מ 18%` (or "use the rate from the document; do not hardcode").

### A12. `onyx-ai/agents/src/tools/pdfTool.ts:60, :68, :77` (and `AI-Task-Manager/artifacts/kobi-agent/src/tools/pdfTool.ts:60, :68, :77`)
```ts
doc.text('מע"מ (17%): ' + data.vat, ...);
output: "Invoice PDF generator → src/pdf/invoice.ts (Hebrew RTL, VAT 17%)"
description: "Generate a Hebrew invoice PDF generator (VAT 17%, אגורות)"
```
**STALE.** PDF row label + tool description both say "17%". **Recommendation:** flip to dynamic `${(rate*100)}%` for the row, "VAT 18%" for the tool description.

### A13. `erp-app/src/components/onboarding-tour.tsx:63, :127`
```ts
'מנוע החשבונאות הישראלי מחשב מע"מ בשיעור 17%, ...'
'מערכת ERP v1.0.0 · מע"מ 17% · מוכנה לשימוש. ...'
```
**STALE.** Onboarding tour shown to every new user announces "VAT 17%". **Recommendation:** flip to `18%`.

### A14. `erp-app/src/pages/sales/deal-room.tsx:282`
```tsx
<TableCell colSpan={4} className="text-xs text-muted-foreground">מע"מ 17%</TableCell>
```
**STALE.** Quote summary row label. **Recommendation:** make dynamic — `מע"מ {(rate*100).toFixed(0)}%`.

### A15. `erp-app/src/pages/finance/accounting-export.tsx:275`
```tsx
<p>• סכום מע"מ (17%)</p>
```
**STALE.** Export-screen description bullet. **Recommendation:** flip to `18%` or drop the literal percent and rely on the actual rate.

### A16. `AI-Task-Manager/artifacts/erp-app/src/utils/money.ts:1`
```ts
export const VAT_RATE = 0.17;
```
**STALE.** Old archived `money.ts` lives under `artifacts/`. If `artifacts/` is built/imported by the AI-Task-Manager runtime, fix or delete. If it's pure scratch, mark dir `.gitignore`d.

---

## B. Test fixtures / structural names (review, mostly keep)

These keys all use the literal token `standard_17` as a **JSON shape name**, not as a rate. The actual rate flows through `vatRate` in `invoice-pdf-generator.js:249`. But the *name* is misleading post-2026.

- `onyx-procurement/src/invoices/invoice-pdf-generator.js:68, 241, 256-257, 264-265, 274, 434` — `breakdown.standard_17.{net,vat}` keyspace.
- `onyx-procurement/test/invoice-pdf-generator.test.js:121, 193-194, 221` — assertions on `vat_breakdown.standard_17`.
- `onyx-procurement/test/fixtures/invoices-pdf.js:70, 118, 160` — fixtures with `standard_17` key.
- `onyx-procurement/docs/INVOICE_PDF.md:33, 38` — doc table referencing `standard_17`.

**Recommendation:** rename `standard_17` → `standard` (or `standard_18`) in a single sweep across generator + tests + docs + any consumer; the *value* is already correctly multiplied by `vatRate`, so no math change. Low-risk rename, high readability win.

---

## C. Test files referencing 17% in describe/it labels

- `onyx-procurement/test/sales/quote-builder.test.js:9, 67, 142` — comments / descriptions "default VAT 17%" / "17% VAT".
- `onyx-procurement/test/realestate/broker-fees.test.js:239` — "computeCommission sale @2% adds 17% VAT".
- `onyx-procurement/test/payroll/pdf-invoice-parser.test.js:371` — `'cross-check accepts legacy 17% VAT rate'` — **HISTORICAL, keep** (this test specifically covers parsing a pre-2026 invoice).
- `onyx-procurement/test/annual-tax-routes.test.js:422` — `// caller-supplied gross (includes 17% VAT)`.

**Recommendation:** review each — if the test value still does `* 0.17` against current production code, flip both the value and the label to 18; if it specifically tests historical input, keep label and rename to "legacy 17%".

---

## D. Database / migrations (keep — historical record)

- `onyx-procurement/supabase/migrations/003-migration-tracking-and-precision.sql:145, 147, 148` — `vat_rates` table seed for `2006-07-01`, `2015-10-01`, **`2026-01-01 'VAT 17% — continues'`**. Lines 145/147 are HISTORICAL. **Line 148 is WRONG** — should insert 0.1800 effective 2026-01-01 (and end the prior 0.1700 row at 2025-12-31). Already covered by `supabase/migrations/00037_vat_rate_18_percent.sql` per AGENT-19; verify 003 is superseded or patch its 2026 row. **STALE / superseded.**

---

## E. Documentation / copy (low priority but user-facing)

- `docs/merged-final/.../USER_GUIDE_HE.md:95, 103, 118` — user guide says default rate 17%. **DOC — update to 18%.**
- `docs/merged-final/.../FAQ.md:66` — FAQ says 17%; root-level `FAQ.md:66` already correctly says 18%. **DOC — sync the two.**
- `AI-Task-Manager/SYSTEM_ARCHITECTURE.md:6539`, `docs/merged/.../SYSTEM_ARCHITECTURE.md:6539` — ASCII diagram "VAT (מע"מ): 17% auto-calculation". **DOC — update.**
- `AI-Task-Manager/DOCUMENTS/VIDEO_SCRIPTS/03_QUOTES_AND_INVOICES.md:65` — script says 17%. **DOC — re-record / re-write.**
- `AI-Task-Manager/DOCUMENTS/ACCOUNTING_INTEGRATION.md:129` — troubleshooting "מע"מ לא מחושב כ-17%". **DOC — update.**
- `docs/merged-final/.../ARCHITECTURE.md:217` — old copy of ARCHITECTURE.md (root is already correct). **DOC — sync.**
- `desktop-tutorial-server/src/services/vat.service.js:4` — `VAT_RATE = 0.18; // ... לפני כן 17%` — **CORRECT, keep.** Comment is historical context.
- `desktop-tutorial-client/src/components/ui/VATCalculator.jsx:1` — `VAT_RATE = 0.18; // Israeli VAT — 18% from 2026-01-01 (was 17%)` — **CORRECT, keep.**
- `ARCHITECTURE.md:217`, `DATA_MODEL.md:152, :310` — already worded correctly ("0.18 default … 0.17 for historical"). **CORRECT, keep.**

---

## F. Non-VAT noise (false positives, ignore)

- `erp-app/src/pages/reports/financial/report-customer-aging.tsx:18` — `change: "17%"` is a **% change in AR aging**, not VAT.
- `erp-app/src/pages/supply-chain/supply-chain-alerts.tsx:41` — "17% פסילה באצווה" = 17% defect rate, not VAT.
- `api-server/package.json.from-AI-Task-Manager:26` — `@types/pdfkit ^0.17.5` is a package version.
- `_qa-reports-25/AGENT-271-logic-vat.md`, `AGENT-302-unit-tests.md`, `AGENT-307-sanity.md`, `AGENT-318-uat-business.md`, `AGENT-194-pdf-parser.md`, `AGENT-19-il-compliance.md`, `_qa-reports/QA-19-blockers.md`, `_qa-reports/AG-X44-petty-cash.md`, `_qa-reports/AG-Y057-broker-fees.md` — **PRIOR QA REPORTS, keep as audit trail.**

---

## Summary table

| # | Path | Severity | Action |
|---|------|----------|--------|
| A1 | `api-server/src/constants.ts:1` | CRITICAL | DELETE / re-export from shared-tax |
| A2 | `api-server/src/lib/contractor-decision.ts:1` | CRITICAL | `1.17` → `1 + getVatRateForDate(d)` |
| A4 | `api-server/src/lib/project-costing-engine.ts:577` | HIGH | fallback `|| 17` → `|| 18` (or helper) |
| A6 | `erp-app/src/pages/procurement/raw-materials/weight-calculator.tsx:18` | HIGH | `0.17` → helper / `0.18` |
| A7 | `erp-app/src/pages/settings.tsx:127` | LOW | `placeholder="17"` → `"18"` |
| A8 | `api-server/.../invoice-calculations.test.ts` (5 lines) | HIGH | drive off `getVatRateForDate(fixtureDate)` |
| A9 | `api-server/.../financial-flow.test.ts` (4 lines) | HIGH | same |
| A10 | `api-server/src/routes/quote-builder.ts:552` | HIGH | dynamic label |
| A11 | `onyx-ai/agents/src/llm/prompts.ts:22` (+2 mirrors) | HIGH | `17%` → `18%` in LLM prompt |
| A12 | `onyx-ai/agents/src/tools/pdfTool.ts:60,68,77` (+2 mirrors) | HIGH | `17%` → dynamic / `18%` |
| A13 | `erp-app/src/components/onboarding-tour.tsx:63,127` | MED | flip to 18% |
| A14 | `erp-app/src/pages/sales/deal-room.tsx:282` | MED | dynamic label |
| A15 | `erp-app/src/pages/finance/accounting-export.tsx:275` | MED | flip to 18% |
| A16 | `AI-Task-Manager/artifacts/erp-app/src/utils/money.ts:1` | LOW | delete or fix |
| B  | `standard_17` keyspace (7 files) | LOW | rename → `standard` |
| C  | test labels (4 files) | LOW | review case-by-case |
| D  | `003-migration-...sql:148` | MED | confirm superseded by 00037 |
| E  | docs (8 files) | LOW | sync copy |
| A3 / A5 | `pricing-engine.ts:100`, `money.ts:6` | KEEP | historical fallback |

**Headline:** ~14 distinct live-code spots still anchored on 17. The two highest-risk are `constants.ts` (whole API server uses it) and `contractor-decision.ts` (1.17 multiplier), both already flagged in earlier waves but not yet fixed.
