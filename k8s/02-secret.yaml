# ═══════════════════════════════════════════════════════════════════════════
# erp-2026-kobi-el — Kubernetes secret
# Mega-ERP Techno-Kol Uzi  ·  "לא מוחקים רק משדרגים ומגדלים"
# Generated by onyx-procurement/src/deploy/manifest-generator.js (AG-X93)
# EN: Hardened defaults — runAsNonRoot, readOnlyRootFilesystem, drop ALL.
# HE: ברירות מחדל מוקשחות — משתמש לא-root, שורש לקריאה בלבד.
# Env reference / קובץ סביבה: .env.example
# ═══════════════════════════════════════════════════════════════════════════
# EN: Populate with `kubectl create secret ...` — do NOT commit real values.
# HE: למלא באמצעות kubectl create secret — אין לדחוף ערכים אמיתיים.
# Reference: .env.example
apiVersion: v1
kind: Secret
metadata:
  name: erp-secrets
  namespace: erp-prod
  labels:
    app.kubernetes.io/part-of: erp-2026-kobi-el
type: Opaque
stringData:
  POSTGRES_USER: erp
  POSTGRES_PASSWORD: __REPLACE_VIA_KUBECTL__
  POSTGRES_DB: erp_main
  JWT_SECRET: __REPLACE_VIA_KUBECTL__
  SESSION_SECRET: __REPLACE_VIA_KUBECTL__
  API_KEY_ADMIN: __REPLACE_VIA_KUBECTL__
  ANTHROPIC_API_KEY: __REPLACE_VIA_KUBECTL__
  OPENAI_API_KEY: __REPLACE_VIA_KUBECTL__
  SUPABASE_SERVICE_KEY: __REPLACE_VIA_KUBECTL__
