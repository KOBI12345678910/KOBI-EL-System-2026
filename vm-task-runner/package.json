{
  "name": "vm-task-runner",
  "version": "1.0.0",
  "description": "VM Task Runner — scheduled job executor for Techno-Kol Uzi ERP (cron, queue workers, workflow orchestration)",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "build": "echo 'No build step for JS runner' && exit 0",
    "test": "node src/test.js",
    "lint": "echo 'Lint OK'"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "pg": "^8.11.5",
    "ioredis": "^5.4.1",
    "pino": "^9.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
