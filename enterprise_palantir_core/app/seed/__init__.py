"""
Seed layer — populates a fresh database with realistic enterprise data
across every domain so the platform boots into a working, demoable state.

The seed is idempotent: running it multiple times is safe.
"""
